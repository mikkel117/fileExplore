use std::{
    fs::{self, DirEntry},
    io,
};

/* #[tauri::command]
pub fn get_standard_dirs() {
    let path =
        fs::read_dir("C:\\Users\\rumbo\\OneDrive\\Skrivebord\\fileExploreTestFoulder").unwrap();
    println!("{:?}", path);
    for entry in path {
        if let Ok(entry) = entry {
            println!("{:?}", entry.path());
        }
    }
} */
